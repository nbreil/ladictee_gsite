{
  "name": "ladictee-gsite",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx serve .",
    "build": "echo 'Static site — no build step required'",
    "preview": "npx serve ."
  }
}
